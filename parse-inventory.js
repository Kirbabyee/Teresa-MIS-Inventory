const fs = require("fs"); const path = require("path"); const espree = require("espree"); const file = path.join(process.cwd(), "src/pages/Inventory.jsx"); const src = fs.readFileSync(file, "utf8"); try { espree.parse(src, { ecmaVersion: 2024, sourceType: "module", jsx: true }); console.log("PARSE_OK"); } catch (e) { console.error("PARSE_ERROR"); console.error(e.message); console.error("line", e.lineNumber, "col", e.column); }
